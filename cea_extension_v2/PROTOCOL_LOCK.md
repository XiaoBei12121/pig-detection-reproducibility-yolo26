# PROTOCOL LOCK — CEA extension v2

Locked at: **2026-09-16T01:25:12**  
Project: `<PIG_PROJECT>`  
Git commit at lock time: `9918d0878e6087be3f4fd99927c8ab4d52393ede` (branch `master`)  
Machine-readable copy: `protocol_lock.yaml` (this document is generated from real artifacts; no value is hand-entered).

> After this lock is committed, **no protocol element may be changed because of results**. The lock is generated by `scripts/phase3_lock_protocol.py`.

## 1. Dataset (fixed)

- Split: leakage-corrected PigDetect split (`dataset/PigDetect_lc/pig_lc.yaml`)
- YAML SHA256: `e29520cdeb2ece4762b316e550d373b8e2dc34f4012ca2c16391c98dc3bd3564`
- train 2411 images / 33127 instances, val 270 / 3614, test 250 / 5436 (official benchmark test)
- train ∩ val images = 0, clip keys = 0
- test ∩ train = 0, test ∩ val = 0
- per-split content SHA256: train `f8aa741990ac6525…`, val `3964365ba15f0455…`, test `7b243f9ef760e910…`
- The official test split is **danuma-only** and the training split contains danuma images: it must never be described as cross-farm, and remains an exploratory same-source test.
- Clip-key limitation: danuma file names expose no frame/video marker, so for danuma the clip key degenerates to the base name.

## 2. Models (fixed)

| variant | description | weight source | SHA256 |
|---|---|---|---|
| E0 | YOLO26s baseline | `<YOLO26_PARENT>\yolo26s.pt` | `646f8bc3fe0a6568…` |
| E5 | YOLO26s + CA + SIoU | `<PIG_PROJECT>\weights\yolo26s_ca_aligned.pt` | `a8ce0ae94d037ea6…` |

E5 always runs with the environment switch `YOLO_SIOU=1`; the CA weights come from the weight-alignment procedure used for every E5 run in the manuscript.

## 3. Seeds (fixed before training)

- Ten matched seeds: **[1, 7, 21, 42, 100, 101, 103, 107, 109, 113]**
- Already trained and audited (pooled only because phase 2 verified full protocol consistency): [42, 1, 7, 21, 100]
- Newly trained in this extension: **[101, 103, 107, 109, 113]**
- No seed may be dropped, replaced, extended or selected after seeing results; E0 and E5 use exactly the same set.

## 4. Training hyperparameters (fixed)

- Explicit: epochs=100, imgsz=640, batch=4, optimizer=MuSGD, lr0=0.01, momentum=0.937
- All remaining arguments are inherited verbatim from the original corrected runs (`args.yaml`), e.g. weight_decay=0.0005, warmup_epochs=3.0, cos_lr=False, patience=100, workers=8, amp=True, deterministic=True, close_mosaic=10
- Keys that differed between the ten audited runs: ['model'] (only the weight source, as expected)
- Command line: `yolo detect train model=<weights> data=<pig_lc.yaml> epochs=100 imgsz=640 batch=4 optimizer=MuSGD seed=<seed> project=<dir> name=<run> device=0`

## 5. Checkpoint rules (fixed)

- `best.pt`: first epoch attaining the maximum validation fitness (strictly-greater update rule).
- `last.pt`: epoch 100.
- Both are kept for every run and analysed separately.

## 6. Evaluation (fixed)

- Implementation: ultralytics YOLO(...).val, identical to scripts/eval_lc_test.py
- Split: test (official PigDetect 250-image benchmark split); imgsz=640, batch=8, workers=0
- Metrics: mAP@0.5, mAP@0.5:0.95, precision, recall; conf/IoU are never tuned
- params: unfused parameters in millions, as in the manuscript Table 9 (E0 9.949M, E5 9.974M)
- GFLOPs: ultralytics get_flops at imgsz=640 (22.7 GFLOPs for both variants)
- Diagnostics/overlap rule: confidence ≥ 0.25, IoU ≥ 0.5
- training-log values are never reused; every checkpoint is re-evaluated from the saved weights

## 7. Statistics (fixed)

- Primary metric: mAP@0.5:0.95 (primary), reported per seed as raw values; per-seed raw values always reported.
- Paired difference: Delta_i = E5_i - E0_i for each matched seed i
- Summary: mean, median, sample SD (n-1), min, max, positive/negative/zero pair counts
- Bootstrap: 100000 paired resamples (with replacement) over the ten deltas, RNG seed 20260916, percentile 95% CI. Exhaustive 10^10 enumeration is explicitly not used.
- Sign-flip: exact enumeration of 1024 configurations (2^10), one- and two-sided.
- Interpretation rules: p > 0.05 is never reported as evidence of equivalence; p < 0.05 is never reported as automatic practical significance; no post-hoc power analysis; no SESOI threshold is defined unless a literature/agricultural-engineering basis exists; raw per-seed deltas are always reported alongside any summary

## 8. RT-DETR-L protocol (fixed)

- Role: non-YOLO architecture-family boundary/context check only; no architectural edits
- Loader: `from ultralytics import RTDETR; RTDETR('rtdetr-l.pt')`; weights `rtdetr-l.pt`
- Data: same corrected split; epochs=100, imgsz=640, seeds=[42, 1, 7]
- Batch: determined by an explicit smoke test (4 -> 2 -> 1 on OOM); the largest stable batch is then used identically for all three seeds
- Optimizer: ultralytics/model-native defaults for RT-DETR; MuSGD of YOLO26 is NOT transferred; the optimizer, lr, scheduler, weight decay, augmentation and AMP actually used are recorded from the training logs
- deterministic=False — deformable attention relies on F.grid_sample, whose CUDA backward is not fully deterministic in the installed PyTorch
- AMP: smoke-tested first; if NaN losses/predictions or matching errors appear, the whole RT-DETR protocol switches to AMP=False for all seeds

## 9. OinkTrack external evaluation rule (fixed)

- external public-dataset evaluation of already frozen models
- Official pipeline: OinkTrack official repository + official make_dataset.py
- official test sequences only; never re-selected
- Checkpoints evaluated: E0/E5 x 10 seeds x {best,last} and RT-DETR-L x 3 seeds x {best,last} at imgsz=640
- Overlap proxy thresholds (reused, not reinvented): {'Low': '< 0.3', 'Medium': '0.3-0.6', 'High': '> 0.6', 'meaning': 'geometric bounding-box overlap proxy, not annotated visible occlusion percentage'}
- Forbidden: any training or fine-tuning on OinkTrack; any threshold/NMS tuning on OinkTrack; any checkpoint selection on OinkTrack; changing the test scenes after seeing results
- Allowed description: “independent external public dataset / external public-dataset evaluation”; forbidden description: “prospective cross-farm cohort / deployment-ready external validation”

## 10. Failure policy (fixed)

- Failed runs are never deleted; they are retrained only from scratch with the identical configuration and the failure is recorded. Already-recorded failure: `lc_E5_seed7` (incidental CUDA OOM, no checkpoint produced; retrained into `lc_E5_seed7-2`).

## 11. Amendment rule

After this lock is committed, no protocol element may be changed because of observed results. Any change would require a new lock file and a new commit, with the old lock preserved.

